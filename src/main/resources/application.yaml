spring:
  application:
    name: stjabah

  datasource:
    url: jdbc:postgresql://localhost:5432/stjabah_db
    username: abdullah
    password: khkmba02
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect